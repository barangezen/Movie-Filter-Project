export interface ITranslationsString {
  header: {
    demoStreaming: string;
    popularTitles: string;
  };
  homeContent: {
    popularMovies: string;
    popularSeries: string;
  };
  queryStatus: {
    loading: string;
    error: string;
  };
  filter: {
    sortBy: string;
    searchPlaceholder: string;
  };
}
