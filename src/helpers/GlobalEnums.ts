export enum BootsrapVariants {
  Secondary = "secondary",
  Dark = "dark",
  OutlineSecondary = "outline-secondary",
  OutlineDark = "outline-dark",
}

export enum FilterOptionsKey {
  AscTitle = "ascTitle",
  DscTitle = "descTitle",
  AscYear = "ascYear",
  DscYear = "descYear",
}

export enum ReactQueryStatus {
  Loading = "loading",
  Error = "error",
  Success = "success",
}

export enum ProgramType {
  Movie = "movie",
  Series = "series",
}
